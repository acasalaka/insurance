spring:
  main:
    allow-circular-references: true
  application:
    name: insurance2206823682
  datasource:
    url: jdbc:postgresql://db:5432/manpromanpro
    username: postgres
    password: secret99
  jpa:
    properties:
      hibernate:
        jdbc:
          lob:
            non_contextual_creation: true
        dialect: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: create
server:
  port: 8080